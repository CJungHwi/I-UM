-- =============================================================================
-- 기존 DB: 테이블명 auto_menu → ium_menu (1회)
-- =============================================================================
-- 선행 조건: 애플리케이션·프로시저가 이미 `get_menu` 등에서 `ium_menu`를 참조하도록 배포된 뒤,
--            이 스크립트로 물리 테이블만 맞춥니다.
--
-- InnoDB: 자기참조 FK는 RENAME 시 메타데이터가 따라옵니다.
-- 이미 `ium_menu`만 있으면 이 파일은 실행하지 마세요.
-- =============================================================================

-- RENAME TABLE `auto_menu` TO `ium_menu`;

-- 위 한 줄의 주석을 해제해 실행하세요. (자동 실행 시 중복 오류 방지 위해 기본은 주석 처리)
